# Navigation Helpers (Compatibility)

This directory mirrors the background navigation utilities to align with the
architecture documentation. The actual implementations live under
`extension/background/navigation/`, and these modules simply re-export them to
avoid duplication.
